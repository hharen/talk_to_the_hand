{
  "name": "talk-to-the-hand",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the Talk to the Hand Jekyll site (Tailwind CSS).",
  "scripts": {
    "build:css": "tailwindcss -i ./src/main.css -o ./assets/css/main.css --minify",
    "watch:css": "tailwindcss -i ./src/main.css -o ./assets/css/main.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
